rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 個人利用(1人専用)の簡易ルール。認証なしで読み書き可能。
    // 本番の不特定多数向けサービスにするならFirebase Authenticationの導入を推奨。
    match /newsItems/{category} {
      allow read, write: if true;
    }
  }
}
